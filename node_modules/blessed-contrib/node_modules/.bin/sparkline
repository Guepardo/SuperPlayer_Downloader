#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../sparkline/bin/sparkline" "$@"
  ret=$?
else 
  node  "$basedir/../sparkline/bin/sparkline" "$@"
  ret=$?
fi
exit $ret
